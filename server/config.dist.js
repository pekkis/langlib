var config = {

    server: {
        'port': 8083
    },

    mysql: {
        'socketPath': '/tmp/mysql.sock',
        'user': 'root',
        'password': 'rootpassu',
        'database': 'langlib'
    }
};

module.exports = config;
