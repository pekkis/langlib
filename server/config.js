var config = {

    server: {
        'port': 8083
    },

    mysql: {
        'socketPath': '/tmp/mysql.sock',
        'user': 'root',
        'password': 'g04753m135',
        'database': 'langlib'
    }
};

module.exports = config;
